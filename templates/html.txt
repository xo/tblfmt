{{ $headers := .Headers }}{{ $rows := .Rows }}<table{{ .Attributes | attr }}>
  <caption>{{ .Title }}</caption>
  <thead>
    <tr>{{ range $i, $h := $headers }}
      <th align="{{ alignString $h.Align | toLower }}">{{ $h }}</th>{{ end }}
    </tr>
  </thead>
  <tbody>{{ range $i, $r := $rows }}
    <tr>{{ range $j, $c := $r  }}
      <td align="{{ alignString $c.Align | toLower }}">{{ $c }}</td>{{ end }}
    </tr>{{ end }}
  </tbody>
</table>
